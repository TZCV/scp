import Vue from "vue";import Vuex from "vuex";Vue.use(Vuex);const files = require.context("./modules", false, /\.js$/);let modules = {	state: {},	mutations: {},	actions: {}};const map = {}for (const key of files.keys()) {	const keyArr = key.split('/')	keyArr.shift() // 移除.	map[keyArr.join('.').replace(/\.js$/g, '')] = files(key).default;}const store = new Vuex.Store({	modules: {		...map, // 示例，正式项目请参考此文件进行修改	}})export default store;